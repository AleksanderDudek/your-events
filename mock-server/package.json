{
  "name": "your-events-mock-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npx json-server --watch db.json --port 3001 --routes routes.json --middlewares cors-middleware.js"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
